enum sofle_layers {
    _QWERTY,
    _COLEMAK,
    _LOWER,
    _RAISE,
    _ADJUST,
};

enum custom_keycodes {
    KC_QWERTY = SAFE_RANGE,
    KC_CLMAK,

    KC_LBRACE, // {
    KC_RBRACE, // }
    KC_LPAREN, // (
    KC_RPAREN, // )
    KC_LABRAC, // [
    KC_RABRAC, // ]

    KC_MAG1,
    KC_MAG2,
    KC_MAG3,
};
